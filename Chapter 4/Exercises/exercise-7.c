/*
    First algorithm:
        9 - ((total - 1) % 10)
    Second algorithm:
        10 - (total % 10)

    This technique doesn't work because not all results coming from both algorithms will be equal
*/