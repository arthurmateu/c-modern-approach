/*
    It will first look for the file in the same folder as the compiled file.
        If it finds something, it will use that one.
        If it can't find anything, it will look at the system header location.

    Given that, it may give some problems, so it's not recommended.
*/