/*
    (a) It will print "Output if DEBUG is defined:", followed by the values of i, j, k, i+j and 2*i+j-k.
    (b) It will print "Output if DEBUG is not defined:".
    (c) If DEBUG isn't defined, it won't be able to access their functions, so it will be ignored.
    (d) Yes, because PRINT_DEBUG is a function of DEBUG, which needs to appear before it.
*/