/*
    (a) The f function has variables b (local), c and d as visible variables
    (b) The g function has variables a and c (local) as visible variables
    (c) The block in which a and d are declared has variables a (local from itself), b (global), c (local from g), and d
    (d) The main function has variables b (global), c and d (both local to the main function)
*/