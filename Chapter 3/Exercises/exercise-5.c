/*
    First, it will look for a float number, which would be 12.3, and it will assign it to x.
    Afterwards, it will look for a digit, which would be 45. It will have to stop there because a dot doesn't make part of integers. This result will be assigned to i.
    Finally, it will look for another float number. Since its next characters are ".6", it will assign 0.6 to y.
*/