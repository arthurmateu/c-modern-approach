/*
    #define DAYS_IN_MONTH 30
    #define HOURS_IN_DAY 24

    float temperature_readings[DAYS_IN_MONTH][HOURS_IN_DAY] = {...}
    float average;

    for (int i = 0; i < DAYS_IN_MONTH; i++){
        for (int j = 0; j < HOURS_IN_DAY; j++){
            average += temperature_readings[i][j];
        }
        printf("Monthly average: %.2f", temperature readings);
        average = 0.00f;
    }
*/